mixin courtCard(court = {})
  .store
    .store__hero
      .store__actions
        if user 
          .store__action.store__action--edit
            a(href=`/courts/${court._id}/delete`)
              != h.icon('trash')
        if user && court.author.equals(user._id)
          .store__action.store__action--edit
            a(href=`/courts/${court._id}/edit`)
              != h.icon('pencil')
      img(src=`/uploads/${court.photo || 'court.jpg'}`)
      h2.title
        a(href=`/court/${court.slug}`) #{court.court}
    .store__details
      p <strong> Defendant:</strong>
      p2 #{court.defendant}
      p <strong>Docket Number:</strong>
      p2 #{court.docketNumber}
      p <strong>Date:</strong> 
      p2 #{h.moment(court.date).format("MMM Do YYYY")} 
      p <strong>Time:</strong> 
      p2 #{court.time}
      p <strong>Department:</strong> 
      p2 #{court.department}
      p <strong>Officer:</strong> 
      p2 #{court.officer}
      p <strong>Disposition:</strong> 
      p2 #{court.tags}

      